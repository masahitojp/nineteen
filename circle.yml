machine:
  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - "~/.gradle"

dependencies:
 pre:
   - cp gradle.properties.sample gradle.properties

test:
 override:
   - ./gradlew clean build -i
